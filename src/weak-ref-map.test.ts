import {describe, expect, it} from 'vitest';
import {WeakRefMap} from './weak-ref-map.js';

// TODO: Add meaningful tests.

describe('WeakRefMap', () => {
  it('should be defined', () => {
    expect(WeakRefMap).toBeDefined();
  });
});
